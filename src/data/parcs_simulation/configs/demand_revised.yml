cost_fuel: gaussian(mu_=0, sigma_=1)
cost_fuel->price: identity()
emotion: deterministic(src/data/parcs_simulation/funcs/demand_revised.py, emotion)
emotion->outcome: identity()
noise_demand: deterministic(src/data/parcs_simulation/funcs/demand_revised.py, noise_demand)
noise_demand->outcome: identity()
noise_price: gaussian(mu_=0, sigma_=1)
noise_price->noise_demand: identity()
noise_price->price: identity()
outcome: deterministic(src/data/parcs_simulation/funcs/demand_revised.py, outcome)
price: deterministic(src/data/parcs_simulation/funcs/demand_revised.py, price)
price->emotion: identity()
price->outcome: identity()
rho: constant(1)
rho->noise_demand: identity()
time: uniform(mu_=5, diff_=10)
time->outcome: identity()
time->price: identity()
