cost_fuel: gaussian(mu_=0, sigma_=1)
cost_fuel->price: identity()
emotion: deterministic(src/data/parcs_simulation/funcs/demand_hetero_1.py, emotion)
emotion->outcome: identity()
noise_demand: deterministic(src/data/parcs_simulation/funcs/demand_hetero_1.py, noise_demand)
noise_demand->outcome: identity()
noise_price: deterministic(src/data/parcs_simulation/funcs/demand_hetero_1.py, noise_price)
noise_price->noise_demand: identity()
noise_price->price: identity()
outcome: deterministic(src/data/parcs_simulation/funcs/demand_hetero_1.py, outcome)
phi_t: deterministic(src/data/parcs_simulation/funcs/demand_hetero_1.py, phi_t)
phi_t->outcome: identity()
phi_t->price: identity()
price: deterministic(src/data/parcs_simulation/funcs/demand_hetero_1.py, price)
price->outcome: identity()
rho: constant(0.1)
rho->noise_demand: identity()
sigma: constant(0.2)
sigma->noise_price: identity()
time: uniform(mu_=5, diff_=10)
time->emotion: identity()
time->noise_price: identity()
time->phi_t: identity()
